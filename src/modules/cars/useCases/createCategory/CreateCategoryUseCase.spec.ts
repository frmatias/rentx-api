import { CreateCategoryUseCase } from "./CreateCategoryUseCase";

describe("Create Category", () => {
    it("Should be able to create a new cateogy", () => {
       const createCategory = new CreateCategoryUseCase();
       
    });
});
